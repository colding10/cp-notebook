#include <bits/stdc++.h>
using namespace std;

void solve() {
    int n, k;
    cin >> n >> k;
    
}

int main() {
    cin.tie(0)->sync_with_stdio(false);
    int tc;
    cin >> tc;

    while (tc--) {
        solve();
    }
}