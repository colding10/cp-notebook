#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void solve() {}

int main() {
    cin.tie(nullptr)->sync_with_stdio(false);

    int tc;
    cin >> tc;
    while (tc--) {
        solve();
    }
}
// look at the paper instead of staring here
